{
  "name": "samurai-usagi",
  "version": "0.1.0",
  "description": "Samurai Usagi: The Pilgrim's Blade — A data-driven JRPG game factory",
  "private": true,
  "type": "commonjs",
  "scripts": {
    "validate": "node tools/validate.js data/books/book-00",
    "validate:all": "node tools/validate-all.js",
    "lint": "eslint tools/**/*.js",
    "lint:fix": "eslint tools/**/*.js --fix",
    "check:json": "node tools/check-json.js",
    "test": "npm run check:json && npm run lint && npm run validate",
    "test:strict": "npm run check:json && npm run lint && npm run validate 2>&1 | grep -c '⚠️' | xargs -I {} test {} -eq 0"
  },
  "devDependencies": {
    "eslint": "^8.56.0"
  },
  "engines": {
    "node": ">=18.0.0"
  }
}
